/**
 * Single place to update when the streaming site changes domain.
 * After editing SENPAI_ORIGIN, run: node update.mjs
 * (updates manifest.json content_scripts matches — required by Chrome.)
 */
const SENPAI_ORIGIN = "https://senpai-stream.casa";
